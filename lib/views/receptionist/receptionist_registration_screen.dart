import 'package:flutter/material.dart';

class ReceptionistRegistrationScreen extends StatelessWidget {
  const ReceptionistRegistrationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Registration Screen Under Development'),
      ),
    );
  }
}
