import 'package:flutter/material.dart';

class FuturePatientListView extends StatelessWidget {
  const FuturePatientListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Future Patient List Screen'),
    );
  }
}
