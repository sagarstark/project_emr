import 'package:flutter/material.dart';

class PastPatientListView extends StatelessWidget {
  const PastPatientListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Past Patient List Screen'),
    );
  }
}
