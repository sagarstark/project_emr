import 'package:flutter/material.dart';

class BillingRecord extends StatelessWidget {
  const BillingRecord({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Billing Record Screen'),
    );
  }
}
