import 'package:flutter/material.dart';

class ClinicalRecord extends StatelessWidget {
  const ClinicalRecord({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Clinical Record Screen'),
    );
  }
}
