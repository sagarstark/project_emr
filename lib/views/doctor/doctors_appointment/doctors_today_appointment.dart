import 'package:flutter/material.dart';

class DoctorsTodayAppointment extends StatelessWidget {
  const DoctorsTodayAppointment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Today Appointment Screen'),
    );
  }
}
