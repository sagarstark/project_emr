import 'package:flutter/material.dart';

class DoctorsFutureAppointment extends StatelessWidget {
  const DoctorsFutureAppointment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Future Appointment Screen'),
    );
  }
}
