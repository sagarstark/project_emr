import 'package:flutter/material.dart';

class DoctorsPastAppointment extends StatelessWidget {
  const DoctorsPastAppointment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Past Appointment Screen'),
    );
  }
}
