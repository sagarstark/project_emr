import 'package:flutter/material.dart';

class FutureOrderList extends StatelessWidget {
  const FutureOrderList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Future'),
    );
  }
}
